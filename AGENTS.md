# Agent Instructions

- Run these checks after every change (in order): `bun run lint`, `bun run knip`, `bun run test`, `bun run build`.
- Periodically check for dead code (unused files/components) and remove it (use `bun run knip`).
